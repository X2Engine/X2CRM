<?php

/**
 * Paths to sample data.
 * 
 * If the path is a directory, it will be cleared of its contents (but the directory
 * itself will remain). If the path is a file, the file will be deleted.
 */
return array(
        'uploads/protected/media'
);
?>
